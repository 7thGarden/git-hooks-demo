const appName: string = "Task Manager";

// SECURITY FLAW: Secret added
const API_KEY: string = "super_secret_password_123";

function initializeApp() {
    // CODE QUALITY FLAW: Debug log in production code
    
}

initializeApp();